<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.combinatorics.shuwashuwa.dao.VolunteerApplicationDao">
    <resultMap type="team.combinatorics.shuwashuwa.model.pojo.VolunteerApplication" id="volunteerApplicationResultMap">
        <result column="formid" jdbcType="INTEGER" property="formid" />
        <result column="comment" jdbcType="VARCHAR" property="comment" />
        <result column="status" jdbcType="TINYINT" property="status" />
    </resultMap>

    <update id="update" parameterType="team.combinatorics.shuwashuwa.model.pojo.VolunteerApplication">
        update volunteer_application
        <trim prefix="SET" suffixOverrides=",">
            <if test="volunteerApplication.formid != null">
                formid = #{volunteerApplication.formid},
            </if>
            <if test="volunteerApplication.comment != null">
                comment = #{volunteerApplication.comment},
            </if>
            <if test="volunteerApplication.status != null">
                status = #{volunteerApplication.status},
            </if>
        </trim>
        where id = #{volunteerApplication.id}
    </update>
</mapper>
