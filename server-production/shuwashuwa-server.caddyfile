{$DOMAIN}
tls /ssl/cert.pem /ssl/cert.key 
log {
  output stdout
  format console
}
route {
  @backend {
     path swagger-ui.html* doc.html* /api/* /img/*
  }
  reverse_proxy @backend http://shuwashuwa:8848
  reverse_proxy /hooks/* http://127.0.0.1:9000
  file_server { root /var/www/html }
}

